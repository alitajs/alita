{
  "name": "boilerplate",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "rm -rf .mfsu && alita dev",
    "build": "alita build",
    "plugin": "alita plugin list"
  },
  "dependencies": {
    "@alita/flow": "workspace:*",
    "@alita/plugins": "workspace:*",
    "alita": "workspace:*"
  }
}
